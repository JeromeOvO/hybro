# Runtime dependencies of the standalone hybro CLI.
#
# These are the third-party distributions the CLI actually imports (entry point
# `configuration_cli`, plus the TUI, setup, catalog, and provider modules it
# reaches). It deliberately excludes the server stack (FastAPI, Motor, tiktoken,
# Pillow, ...) that only `main.py` needs, which keeps the bundle small.
#
# Verified by importing the CLI closure and mapping the used modules back to
# installed distributions. `common/config/loader.py` needs only pydantic;
# pydantic-settings is a backend-server dependency and is not imported here.
pydantic
httpx
httpx-sse
openai
# Provider structured output is validated with JSON Schema.
jsonschema
# `config migrate` reads the legacy YAML file and an optional dotenv file.
pyyaml
python-dotenv
